{
  "name": "telweb-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build needed for frontend' && cd backend && npm install",
    "install-backend": "cd backend && npm install"
  }
}
